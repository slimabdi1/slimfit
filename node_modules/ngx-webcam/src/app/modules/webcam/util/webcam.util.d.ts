export declare class WebcamUtil {
    /**
     * Lists available videoInput devices
     * @returns {Promise<MediaDeviceInfo[]>}
     */
    static getAvailableVideoInputs(): Promise<MediaDeviceInfo[]>;
}
